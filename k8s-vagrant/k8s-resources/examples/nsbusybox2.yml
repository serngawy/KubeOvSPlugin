apiVersion: v1
kind: Pod
metadata:
  name: nsbusybox2
  namespace: sock-shop
spec:
  containers:
  - image: busybox
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
    name: busybox2
  restartPolicy: Always
